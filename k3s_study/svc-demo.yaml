apiVersion: v1
kind: Service
metadata:
  name: web-svc
spec:
  selector:
    app: web                  # 选中带有 app=web 标签的 Pod
  ports:
  - port: 80                  # Service 对外暴露的端口
    targetPort: 80            # Pod 上监听的端口
  type: ClusterIP             # 仅集群内部可访问
